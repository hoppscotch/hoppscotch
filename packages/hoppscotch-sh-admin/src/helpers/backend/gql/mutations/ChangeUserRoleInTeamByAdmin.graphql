mutation ChangeUserRoleInTeamByAdmin(
  $userUID: ID!
  $teamID: ID!
  $newRole: TeamMemberRole!
) {
  changeUserRoleInTeamByAdmin(
    userUID: $userUID
    teamID: $teamID
    newRole: $newRole
  ) {
    membershipID
    role
  }
}
