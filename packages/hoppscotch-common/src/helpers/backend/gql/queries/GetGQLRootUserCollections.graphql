query GetGQLRootUserCollections($cursor: ID, $take: Int) {
  rootGQLUserCollections(cursor: $cursor, take: $take) {
    id
    title
    data
    type
    parent {
      id
    }
    requests {
      id
      title
      request
      type
      collectionID
    }
    childrenGQL {
      id
      title
      data
      type
      parent {
        id
      }
      requests {
        id
        title
        request
        type
        collectionID
      }
    }
  }
}