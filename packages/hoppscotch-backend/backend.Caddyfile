{
	admin off
	persist_config off
}

:{$HOPP_ALTERNATE_PORT:80} :3170 {
	@mock {
		header_regexp host Host ^[^.]+\.mock\..*$
	}
	
	handle @mock {
		rewrite * /mock{uri}
		reverse_proxy localhost:8080
	}
	
	handle {
		reverse_proxy localhost:8080
	}
}
