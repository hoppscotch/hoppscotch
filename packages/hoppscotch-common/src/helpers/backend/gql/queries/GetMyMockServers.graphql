query GetMyMockServers {
  myMockServers {
    id
    name
    subdomain
    user {
      uid
    }
    collection {
      id
    }
    isActive
    createdOn
    updatedOn
    collection {
      id
      title
    }
  }
}
