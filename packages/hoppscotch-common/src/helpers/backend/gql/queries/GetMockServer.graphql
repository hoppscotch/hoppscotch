query GetMockServer($id: String!) {
  mockServer(id: $id) {
    id
    name
    subdomain
    user {
      uid
    }
    collection {
      id
    }
    isActive
    createdOn
    updatedOn
    collection {
      id
      title
    }
  }
}
