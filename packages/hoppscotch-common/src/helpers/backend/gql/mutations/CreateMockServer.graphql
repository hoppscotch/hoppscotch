mutation CreateMockServer($input: CreateMockServerInput!) {
  createMockServer(input: $input) {
    id
    name
    subdomain
    user {
      uid
    }
    collection {
      id
    }
    isActive
    createdOn
    updatedOn
  }
}
