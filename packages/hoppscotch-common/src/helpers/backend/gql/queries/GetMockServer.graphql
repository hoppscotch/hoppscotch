query GetMockServer($id: String!) {
  mockServer(id: $id) {
    id
    name
    subdomain
    workspaceType
    workspaceID
    delayInMs
    isPublic
    isActive
    createdOn
    updatedOn
    creator {
      uid
    }
    collection {
      id
      title
    }
  }
}
