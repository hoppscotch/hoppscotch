query GetUserRootCollections {
  # the frontend doesn't paginate right now, so giving take a big enough value to get all collections at once
  rootRESTUserCollections(take: 99999) {
    id
    title
    type
    data
    childrenREST {
      id
      title
      type
      data
    }
  }
}
