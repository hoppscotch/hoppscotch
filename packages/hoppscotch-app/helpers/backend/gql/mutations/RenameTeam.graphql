mutation RenameTeam($newName: String!, $teamID: ID!) {
  renameTeam(newName: $newName, teamID: $teamID) {
    id
    name
    members {
      membershipID
      user {
        uid
      }
      role
    }
  }
}