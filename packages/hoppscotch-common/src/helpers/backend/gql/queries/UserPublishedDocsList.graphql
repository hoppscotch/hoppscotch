query UserPublishedDocsList($skip: Int!, $take: Int!) {
  userPublishedDocsList(skip: $skip, take: $take) {
    id
    title
    version
    autoSync
    url
    documentTree
    collection {
      id
    }
    createdOn
    updatedOn
  }
}
