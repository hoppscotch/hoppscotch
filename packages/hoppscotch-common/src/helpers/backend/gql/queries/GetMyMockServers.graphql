query GetMyMockServers($skip: Int, $take: Int) {
  myMockServers(skip: $skip, take: $take) {
    id
    name
    subdomain
    workspaceType
    workspaceID
    delayInMs
    isPublic
    isActive
    createdOn
    updatedOn
    creator {
      uid
    }
    collection {
      id
      title
    }
  }
}
